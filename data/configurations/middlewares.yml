# Dynamic configuration
http:

  # All middlewares
  middlewares:
  
    # Website security
    secureHeaders:
      headers:
        sslRedirect: true
        forceSTSHeader: true
        stsIncludeSubdomains: true
        stsPreload: true
        stsSeconds: 31536000

    # Basic user authentification (username/password)
    user-auth:
      basicAuth:
        usersFile: "/shared/.htpasswd" # Mount the volume through docker-compose.yml
        realm: "Traefik 2 Basic Auth"

    # Mitigate DDOS Attacks
    rate-limit:
      rateLimit:
        average: 100
        burst: 50


# Uncoment for different services

    # Websocket - Bitwarden
#    bw-stripPrefix:
#      stripPrefix:
#        prefixes:
#          - "/notifications/hub"
#        forceSlash: false

    # WWW - Redirect1
#    www-redirect1:
#      redirectRegex:
#        regex: "^https://domain1/(.*)"
#        replacement: "https://www.domain1/${1}"

    # WWW - Redirect2
#    www-redirect2:
#      redirectRegex:
#        regex: "^https://domain2/(.*)"
#        replacement: "https://www.domain2/${1}"

http:
  routers:
    nginx:
      service: justluna
      middlewares:
      entryPoints:
        - "websecure"
      rule: "Host(`justluna.xyz`)"

  services:
    justluna:
      loadBalancer:
        servers:
          - url: "http://192.168.1.182:80"

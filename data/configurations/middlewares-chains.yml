http:
  middlewares:
    chain-no-auth:
      chain:
        middlewares:
          - rate-limit
          - secure-headers

    chain-basic-auth:
      chain:
        middlewares:
          - rate-limit
          - secure-headers
          - basic-auth

#    chain-oauth:
#      chain:
#        middlewares:
#          - middlewares-rate-limit
#          - middlewares-secure-headers
#          - middlewares-oauth
